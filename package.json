{
  "name": "redirect-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "wrangler": "^4.77.0"
  }
}
