{
  "name": "redirect-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.53.0"
  }
}
